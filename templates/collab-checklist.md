# Collaboration Checklist — <模块名称>

> Task ID: `<task-xxx>`  
> Module: `<前端模块名称>`  
> Execution Type: `parallel | serial | shared-sync`  
> Owner Agent: `<agent_id>`  
> Validator Agent: `<agent_id>`  
> Date: `YYYY-MM-DD`

## 1. 模块归属

- 所属页面：`<页面名称>`
- 模块职责：`<一句话描述这个模块做什么>`
- 依赖模块：`<上游模块列表>`
- 被依赖模块：`<下游模块列表>`

## 2. 上游契约（我从谁那里拿数据）

| 上游模块 | 触发条件 | 数据格式 | 异常处理 |
|---|---|---|---|
| `<module_a>` | `<事件/轮询/推送>` | `<JSON/字段定义>` | `<降级策略>` |

## 3. 下游契约（我给谁提供数据）

| 下游模块 | 输出格式 | 触发条件 | 异常处理 |
|---|---|---|---|
| `<module_b>` | `<JSON/字段定义>` | `<事件/状态变化>` | `<降级策略>` |

## 4. 组件清单

| 组件名 | 数据来源 | 触发条件 | 状态变化 |
|---|---|---|---|
| `<ComponentA>` | `<API/Store/Props>` | `<用户操作/数据到达>` | `<state_a → state_b>` |

## 5. 任务拆解

| # | 子任务 | 类型 | 依赖 | 预估复杂度 | Owner |
|---|---|---|---|---|---|
| 1 | `<子任务描述>` | `parallel/serial/shared-sync` | `前置任务#` | `H/M/L` | `<agent>` |

## 6. 验收标准

### 6.1 功能验收

- [ ] `<可执行的验收条件 1>`（例：打开页面后行情面板在 2s 内加载数据）
- [ ] `<可执行的验收条件 2>`
- [ ] `<可执行的验收条件 3>`

### 6.2 契约验收

- [ ] 上游数据缺失时，模块显示降级状态而非崩溃
- [ ] 输出数据格式与模块契约定义一致
- [ ] 状态同步机制正常工作（如有共享状态）

### 6.3 质量验收

- [ ] 代码通过 lint/typecheck
- [ ] 单元测试覆盖关键路径
- [ ] 无越权修改共享边界/冻结契约

## 7. 防漂移检查

在开发过程中，每次提交前自查：

- 本次修改是否对应本清单中的某个子任务？
- 是否触碰了上游/下游契约中定义的数据格式？
- 是否超出了本模块的职责范围？
- 验收标准是否仍然可达成？

## 8. 变更历史

| 时间 | 变更内容 | 变更人 |
|---|---|---|
| `YYYY-MM-DD` | 初始创建 | `<agent_id>` |
